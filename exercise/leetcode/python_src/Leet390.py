class Solution(object):
    def lastRemaining(self, n):
        """
        :type n: int
        :rtype: int
        """
        head = 0
        left = True
        step = 2
        while  > 0:
            if head % step == 0:
                
